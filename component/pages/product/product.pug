section(class="bg-[#123411] flex flex-col gap-[20px] justify-stretch min-h-[100vh] w-full max: px-[40px]")
    notification-element
    overlay-element
    header-element
    div.foodContainer(class="h-full")
        holder-element
            //- p(slot="test") it works yayy
    if state.model.showFooter
        footer-element