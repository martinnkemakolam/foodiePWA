section(class="bg-[#123411] flex min-h-[100%] h-[100%] flex-col px-[40px] gap-[20px]")
    notification-element
    header-element
    if state.model.authObject.email === "nkemakolam.martin@gmail.com"
        cmsholder-element
    else
        div(class="bg-[#fff] p-4 rounded h-[100%] flex flex-col") 
            signin-element
    if state.model.showFooter
        footer-element